server {
    listen 80;
    location / {
        root /usr/share/nginx/html;
        index index.html;
        # Essential for SPAs to avoid 404 on refresh
        try_files $uri $uri/ /index.html;
    }
}
